import React from 'react';
import { ProgressWrapper } from './style';

const ProgressWrap = ({ children }) => (
    <ProgressWrapper>
        { children }
    </ProgressWrapper>
);

export { ProgressWrap }